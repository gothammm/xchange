import styled from 'styled-components';
import FlexContainer from './FlexContainer';

const CenterPageContainer = styled(FlexContainer)`
  justify-content: center;
  align-items: center;
  flex: 1;
`;

export default CenterPageContainer;
